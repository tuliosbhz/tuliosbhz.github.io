{
  "name": "personal-webpage",
  "version": "1.0.0",
  "description": "A personal webpage project.",
  "main": "index.html",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp index.html dist/ && cp -r src/styles dist/ && cp -r src/scripts dist/",
    "dev": "serve ."
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.0.0"
  }
}