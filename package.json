{
  "name": "personal-webpage",
  "version": "1.0.0",
  "description": "A personal webpage project.",
  "main": "index.html",
  "scripts": {
    "start": "cross-env NODE_ENV=development node scripts/build.js && serve dist",
    "dev": "cross-env NODE_ENV=development node scripts/build.js && serve dist",
    "build": "cross-env NODE_ENV=production node scripts/build.js",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^11.3.0",
    "serve": "^14.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0"
  }
}
